# Implementation summary

The empty repository skeleton was replaced by config-driven experiment code for
all dependency-critical stages: metadata/lesion integrity, descriptors,
balanced matching, coverage and balance, exact counterfactual construction,
model training, HCS/HCE, statistics, controls, robustness, OOD aggregation, and
secondary mitigation setup. Critical scientific logic has synthetic tests.

The repository does not include datasets, model weights, or empirical results.
Large-dataset network downloads are disabled in code.
