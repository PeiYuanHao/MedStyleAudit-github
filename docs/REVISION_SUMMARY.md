# Final execution conversion

The repository is now code/config/test only. The protocol is canonicalized and
hashed, matching emits feature and reuse diagnostics, preflight is automatic and
fail-closed, hospital 2 has an explicit clean-commit unlock, aggregation records
missing runs, and the server runner resumes by stage and seed. Derived artifacts
are checksummed into the private Hugging Face Dataset repository; raw medical
data remains local.
