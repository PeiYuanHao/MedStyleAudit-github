# Final execution status

The required protocol and execution paths are frozen. Remaining work is running
the suite on the server, not adding methodology.

Lesion-aware analysis runs only after validated WSI/XML alignment; otherwise its
status is `unavailable`. Optional secondary work excluded from the required suite:
appearance-matched level 6, GroupDRO, HED augmentation, context-consistency
mitigation, and a second external medical dataset.
