experiment: planted_shortcut_control
seed: 42
output_dir: ${MEDSTYLE_OUTPUT_ROOT}/audits/planted_shortcut
control:
  strengths: [0.0, 0.25, 0.5, 0.75, 1.0]
  cue: corner_square
  cue_size: 8
  permutation: true
  epochs: 20
