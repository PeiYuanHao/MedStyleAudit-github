experiment: context_randomized_control
seed: 42
output_dir: ${MEDSTYLE_OUTPUT_ROOT}/audits/context_randomized
control:
  assignment: source_label_independent_peripheral_donor
  donor_reuse_cap: 20
  exclude_same_physical_id: true
  epochs: 20
